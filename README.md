# Описание

Простой финансовый трекер, отображающий расходы средств. Включает в  свой функционал возможности фильтрации расходов по категориям и временному периоду.

## Установка и запуск
Для запуска потребуется nodejs и docker-compose, а так же подключение к сети интернет (для скачивания пакетов)
### Запуск бэкенда
Используйте npm для установки зависимостей:

```bash
npm i
```
Установите необходимые образы и запустите контейнеры:
```bash
docker compose up
```
Создайте и заполните данными файл .env по шаблону example.env в корне проекта

Запустите nodejs приложение:
```bash
npm start
```
### Запуск фронтенда
Перейдите в папку frontend и установите зависимости:
```
cd frontend
npm install
```
Запустите  developement build и локальный веб-сервер:
```
npm run serve
```
## Использование

После запуска фронта и бэка, а так же PostgreSQL, веб-интерфейс будет доступен по локальному адресу `http://localhost:8080/`, API доступен по адресу `http://localhost:8085`.

## TODO
- ✅ набросать шаблон приложения
- ✅ реализовать CRUD операции для сущности "Категории"
- ✅ реализовать CRUD операции для сущности "Расходы"

(опционально):
 
- добавить регистрацию пользователей, аутентификацию и авторизацию
